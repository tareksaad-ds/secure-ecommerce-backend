services:
  db:
    image: postgres:15
    container_name: secure_ecommerce_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: always
    # Backend API
  api:
    build: .
    container_name: secure_ecommerce_api
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      PORT: 3009
    ports:
      - "3009:3009"
    depends_on:
      - db
    restart: always

volumes:
  db_data:
